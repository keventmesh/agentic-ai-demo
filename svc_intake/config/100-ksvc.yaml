apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: svc-intake
  namespace: keventmesh
spec:
  template:
    metadata:
      labels:
        app: svc-intake
    spec:
      containers:
        - name: user-container
          image: svc-intake:latest
          env:
            - name: PORT
              value: "8080"
          ports:
            - containerPort: 8080
              name: http1
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
